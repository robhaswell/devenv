alias devhere='docker run -ti -v $(pwd):/$(basename $(pwd)) -v ~/.ssh:/root/.ssh -v $(dirname $SSH_AUTH_SOCK):$(dirname $SSH_AUTH_SOCK) -e SSH_AUTH_SOCK=$SSH_AUTH_SOCK -w /$(basename $(pwd)) robhaswell/devenv bash'

# vim: syntax=sh:
